# AS Code — Python Dependencies
# Ultra-minimal dependency set for edge inference

# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Optional: System monitoring (lightweight)
psutil>=6.0.0

# Optional: LiteRT model inference (CompiledModel API)
# ai-edge-litert>=1.2.0

# Optional: LiteRT-LM Python API (when Windows support arrives)
# litert-lm-api-nightly

# YAML config parsing
pyyaml>=6.0.0

# HTTP client for model downloads
httpx>=0.27.0

# Document parsing (RAG — upload TXT/PDF/DOCX)
python-multipart>=0.0.9
pypdf==4.0.1
python-docx==0.8.11

# RAG — NotebookLM Pipeline
sqlalchemy>=2.0.0              # SQLite persistence for doc/chunk metadata
sentence-transformers>=2.2.2   # BAAI/bge-small-en-v1.5 local embeddings
faiss-cpu>=1.7.4               # Vector search index
rank-bm25>=0.2.2               # Keyword retrieval for hybrid mode
numpy>=1.24.0                  # Embedding arrays
anthropic>=0.32.1              # Claude API (optional, future)
